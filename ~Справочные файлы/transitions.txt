#переходы, включая заимствованные http://ru.renpypedia.shoutwiki.com/wiki/%D0%9F%D0%B5%D1%80%D0%B5%D1%85%D0%BE%D0%B4%D1%8B_(Transitions)
with fade              # мягкий переход через чёрное (настраивается)
with pixellate         # переход через пикселизацию
with vpunch            # переход с верткальной встряской экрана
with hpunch            # переход с горизонтальной встряской экрана
with blinds            # вертикальные жалюзи (без чёрного)
with squares           # небольшие квадраты (без чёрного)
with wipeleft          # старая сцена стирается влево, под ней уже стоит новая сцена (также используется right, up, down)
with slideleft         # новая сцена надвигается справа налево на старую (также используется right, up, down)
with slideawayleft     # старая сцена уезжает влево, под ней уже стоит новая сцена (также используется right, up, down)
with irisin            # новая сцена поглощает старую
with irisout           # новая сцена распирает старую

with diam              # для перехода используется полутоновая карта см. init
with fdiam             # для перехода используется полутоновая карта см. init
with fulldiam          # для перехода используется полутоновая карта см. init
with clock_l           # сцена стирается против часовой стрелке
with clock_r           # сцена стирается по часовой стрелке
with joff_l            # сцена стирается против часовой стрелке (через чёрный экран)
with joff_r            # сцена стирается по часовой стрелке (через чёрный экран)
with blind_d           # для перехода используется полутоновая карта см. init
with blind_l           # жалюзи через чёрное
with blind_r           # жалюзи через чёрное
with touch             # круги на воде
with guess_on          # лицо закрыли ладошками (специфичный, для ухода в чёрное)
with guess_off         # убрали ладошки от лица (специфичный, для выхода из чёрного)


